# viスタイルのキーバインドを有効にする
setw -g mode-keys vi

# クリップボードを連携させたい場合（macOS向け）
bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"

# カラースキームやステータスバーなどお好みで追加できます
set -g mouse on  # マウスでペイン選択やスクロール可にする（オプション）
